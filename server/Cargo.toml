[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio-macros = "2.3.0"
tokio = { version = "1.38.0", features = ["full"] }
utils = { path = "../utils" }

# Add any other dependencies you may need