[package]
name = "utils"
version = "0.0.0"
edition = "2021"

[dependencies]
tokio = { version = "1.38.0", features = ["full"] }
wait-timeout = "0.2.0"
regex = "1.10.5"
serial_test = "3.1.1"

# Add any other dependencies you may need